class URLS {
	mainRoot = '/main'

	TASK_MASTER = `${this.mainRoot}/task-master`
	ECONO_EYE = `${this.mainRoot}/econo-eye`
	DREAM_TIME = `${this.mainRoot}/dream-time`
}

export const PAGES_URL = new URLS()
